apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.identity.server.name }}
  labels:
{{ include "micrshopping.labels" . | indent 4 }}
spec:
  type: {{ .Values.identity.service.type }}
  ports:
    - name: "http"
      port: 80
      protocol: TCP
      targetPort: {{ .Values.identity.service.httpPort }}
    - name: "https"
      port: 443
      protocol: TCP
      targetPort: {{ .Values.identity.service.httpsPort }}
      
  selector:
    app.kubernetes.io/name: {{ include "micrshopping.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
