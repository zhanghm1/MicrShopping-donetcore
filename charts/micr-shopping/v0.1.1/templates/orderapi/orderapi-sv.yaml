apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name  }}
  labels:
{{ include "micrshopping.labels" . | indent 4 }}
spec:
  type: {{ .Values.orderapi.service.type }}
  ports:
    - port: {{ .Values.orderapi.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.orderapi.service.httpsPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "micrshopping.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
