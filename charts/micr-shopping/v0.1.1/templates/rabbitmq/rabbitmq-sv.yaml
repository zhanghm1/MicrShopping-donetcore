apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name  }}
  labels:
{{ include "micrshopping.labels" . | indent 4 }}
spec:
  type: {{ .Values.rabbitmq.service.type }}
  ports:
  - port: {{ .Values.rabbitmq.httpPort1 }}
      targetPort: http
      protocol: TCP
      name: http
  - port: {{ .Values.rabbitmq.httpPort2 }}
      targetPort: http
      protocol: TCP
      name: http
  - port: {{ .Values.rabbitmq.httpPort3 }}
      targetPort: http
      protocol: TCP
      name: http
    
  selector:
    app.kubernetes.io/name: {{ include "micrshopping.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
