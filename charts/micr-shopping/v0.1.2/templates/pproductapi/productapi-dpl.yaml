apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.productapi.deployment_name }}
  labels:
{{ include "micrshopping.labels" . | indent 4 }}
    component: {{ .Values.productapi.deployment_name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "micrshopping.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "micrshopping.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: {{ .Chart.Name }}
        securityContext:
          {{- toYaml .Values.productapi.securityContext | nindent 12 }}
        image: "{{ .Values.repositoryUrl }}/{{ .Values.repositoryProject }}/{{ .Values.productapi.image.repository }}:{{ .Chart.ApiVersion }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        env:
        - name: secret
          valueFrom:
            secretKeyRef:
              key: secret
              name: {{ template "micrshopping.secretname" . }}
              optional: false
        envFrom:
        - configMapRef:
            name: {{ template "micrshopping.configmapname" . }}
            optional: false
        - secretRef:
            name: {{ template "micrshopping.secretname" . }}
        ports:
          - name: http
            containerPort: 80
            protocol: TCP
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
